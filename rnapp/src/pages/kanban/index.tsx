import React, { Component } from 'react';
import { View } from 'react-native';
import { YouText } from 'you-ui';

export default class Kanban extends Component {
  render() {
    return (
      <View>
        <YouText>kanban</YouText>
      </View>
    );
  }
}

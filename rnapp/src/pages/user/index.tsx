import React, { Component } from 'react';
import { View } from 'react-native';
import { YouText } from 'you-ui';

export default class User extends Component {
  render() {
    return (
      <View>
        <YouText>user</YouText>
      </View>
    );
  }
}
